# dj-coach

A project to get real time feedback on my DJ transitions and sets.

```bash
dj-coach
├── journal         # Going to keep a log of my progress as I go
└── prototypes      # Throwaway code for understanding concepts
```

# Python Setup

```bash
brew install mise
brew install uv
brew install portaudio      # For mac, need to consider windows...
brew install blackhole-2ch
```
